"use strict";
(self["webpackChunkwordpress_webpack_workflow"] = self["webpackChunkwordpress_webpack_workflow"] || []).push([["assets_src_js_scripts_animations_js"],{

/***/ "./assets/src/js/scripts/animations.js":
/*!*********************************************!*\
  !*** ./assets/src/js/scripts/animations.js ***!
  \*********************************************/
/***/ (function(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   initAnimations: function() { return /* binding */ initAnimations; },
/* harmony export */   refreshAnimations: function() { return /* binding */ refreshAnimations; }
/* harmony export */ });
/* harmony import */ var _event_utilities__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./__event-utilities */ "./assets/src/js/scripts/__event-utilities.js");
/* harmony import */ var gsap__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! gsap */ "./node_modules/gsap/index.js");
/* harmony import */ var gsap_ScrollTrigger__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! gsap/ScrollTrigger */ "./node_modules/gsap/ScrollTrigger.js");
function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t.return || t.return(); } finally { if (u) throw o; } } }; }
function _slicedToArray(r, e) { return _arrayWithHoles(r) || _iterableToArrayLimit(r, e) || _unsupportedIterableToArray(r, e) || _nonIterableRest(); }
function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _iterableToArrayLimit(r, l) { var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (null != t) { var e, n, i, u, a = [], f = !0, o = !1; try { if (i = (t = t.call(r)).next, 0 === l) { if (Object(t) !== t) return; f = !1; } else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0); } catch (r) { o = !0, n = r; } finally { try { if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return; } finally { if (o) throw n; } } return a; } }
function _arrayWithHoles(r) { if (Array.isArray(r)) return r; }



function initAnimations() {
  init();
  setUpHeader();
  setUpFooter();
  createHeroBlocks();
  animateHero();
  animateElements();
  animatePricingCards();
  initMagneticButtons();
  $('html').removeClass('no-gsap').addClass('gsap');
}
function init() {
  gsap__WEBPACK_IMPORTED_MODULE_1__.gsap.registerPlugin(gsap_ScrollTrigger__WEBPACK_IMPORTED_MODULE_2__.ScrollTrigger);
  gsap__WEBPACK_IMPORTED_MODULE_1__.gsap.config({
    nullTargetWarn: false,
    force3D: false
  });
  gsap__WEBPACK_IMPORTED_MODULE_1__.gsap.defaults({
    ease: "power2.out",
    duration: 0.75
  });
  gsap_ScrollTrigger__WEBPACK_IMPORTED_MODULE_2__.ScrollTrigger.config({
    limitCallbacks: true
  });

  // refresh trigger points for Safari to calculate correct height of footer
  setTimeout(function () {
    gsap_ScrollTrigger__WEBPACK_IMPORTED_MODULE_2__.ScrollTrigger.refresh();
  }, 1000);
}
function setUpHeader() {
  var $siteHeader = $('#js-site-header');
  function storeHeaderHeight() {
    $('html').css('--header-height', "".concat(Math.ceil($siteHeader.outerHeight(true)), "px"));
  }
  storeHeaderHeight();
  $(window).on('resize', (0,_event_utilities__WEBPACK_IMPORTED_MODULE_0__.debounce)(storeHeaderHeight, 150));
  $(window).on('scroll', (0,_event_utilities__WEBPACK_IMPORTED_MODULE_0__.throttle)(storeHeaderHeight, 100));
}
function setUpFooter() {
  var $siteFooter = $('.site-footer');
  function storeFooterHeight() {
    $('html').css('--footer-height', "".concat(Math.ceil($siteFooter.outerHeight(true)), "px"));
  }
  storeFooterHeight();
}

// ─── HERO BLOCKS ───────────────────────────────────────────────────────────────
// Slowly falling isometric cubes — echoes the block-based logo concept.
// Drawn on a canvas injected behind all hero content.

function createHeroBlocks() {
  var wrapper = document.querySelector('.hero-wrapper');
  if (!wrapper) return;
  var canvas = document.createElement('canvas');
  canvas.id = 'hero-blocks';
  canvas.setAttribute('aria-hidden', 'true');
  wrapper.insertBefore(canvas, wrapper.firstChild);
  var ctx = canvas.getContext('2d');

  // Brand-palette cube colors [r, g, b]
  var PALETTES = [[2, 116, 165],
  // #0274a5 – mid blue
  [2, 172, 233],
  // #02ace9 – light blue
  [14, 44, 72] // slightly lighter than darkblue
  ];
  var W,
    H,
    blocks = [];
  function resize() {
    W = canvas.width = wrapper.offsetWidth;
    H = canvas.height = wrapper.offsetHeight;
  }

  // Isometric cube centered at (cx, cy) with half-size s — outline only
  function drawIsoCube(cx, cy, s, rgb, alpha) {
    var _rgb = _slicedToArray(rgb, 3),
      r = _rgb[0],
      g = _rgb[1],
      b = _rgb[2];
    var c = 0.866; // cos(30°)

    var top = [cx, cy - s];
    var tr = [cx + s * c, cy - s * 0.5];
    var br = [cx + s * c, cy + s * 0.5];
    var bot = [cx, cy + s];
    var bl = [cx - s * c, cy + s * 0.5];
    var tl = [cx - s * c, cy - s * 0.5];
    var ctr = [cx, cy];
    ctx.strokeStyle = "rgba(".concat(r, ",").concat(g, ",").concat(b, ",").concat(alpha, ")");
    ctx.lineWidth = 1;
    ctx.beginPath();
    // Outer hexagon
    ctx.moveTo(top[0], top[1]);
    ctx.lineTo(tr[0], tr[1]);
    ctx.lineTo(br[0], br[1]);
    ctx.lineTo(bot[0], bot[1]);
    ctx.lineTo(bl[0], bl[1]);
    ctx.lineTo(tl[0], tl[1]);
    ctx.closePath();
    // Inner Y
    ctx.moveTo(ctr[0], ctr[1]);
    ctx.lineTo(tr[0], tr[1]);
    ctx.moveTo(ctr[0], ctr[1]);
    ctx.lineTo(tl[0], tl[1]);
    ctx.moveTo(ctr[0], ctr[1]);
    ctx.lineTo(bot[0], bot[1]);
    ctx.stroke();
  }

  // Edge-weighted x — 42% left zone, 42% right zone, 16% sparse center
  function spawnX() {
    var r = Math.random();
    if (r < 0.42) return Math.random() * W * 0.3;
    if (r < 0.84) return W * 0.7 + Math.random() * W * 0.3;
    return W * 0.2 + Math.random() * W * 0.6;
  }
  function makeBlock(preplaced) {
    var rgb = PALETTES[Math.floor(Math.random() * PALETTES.length)];
    var s = 30 + Math.random() * 30; // 30–60 px half-size
    return {
      x: spawnX(),
      y: preplaced ? Math.random() * H : -s * 2 - Math.random() * H * 0.4,
      vx: 0,
      vy: 0.08 + Math.random() * 0.16,
      s: s,
      rgb: rgb,
      alpha: 0.3 + Math.random() * 0.3
    };
  }
  function targetCount() {
    return Math.min(Math.floor(W * H / 14000), 50);
  }
  function tick() {
    ctx.clearRect(0, 0, W, H);
    var _iterator = _createForOfIteratorHelper(blocks),
      _step;
    try {
      for (_iterator.s(); !(_step = _iterator.n()).done;) {
        var b = _step.value;
        drawIsoCube(b.x, b.y, b.s, b.rgb, b.alpha);
        b.y += b.vy;
        if (b.y - b.s * 1.2 > H) {
          b.y = -b.s * 2;
          b.vy = 0.08 + Math.random() * 0.16;
        }
      }

      // Vignette — darkens corners so text area stays crisp
    } catch (err) {
      _iterator.e(err);
    } finally {
      _iterator.f();
    }
    var vg = ctx.createRadialGradient(W * 0.5, H * 0.5, H * 0.15, W * 0.5, H * 0.5, H * 0.85);
    vg.addColorStop(0, 'rgba(6,15,30,0)');
    vg.addColorStop(1, 'rgba(6,15,30,0.52)');
    ctx.fillStyle = vg;
    ctx.fillRect(0, 0, W, H);
    requestAnimationFrame(tick);
  }
  resize();
  blocks = Array.from({
    length: targetCount()
  }, function () {
    return makeBlock(true);
  });
  tick();
  window.addEventListener('resize', (0,_event_utilities__WEBPACK_IMPORTED_MODULE_0__.debounce)(function () {
    resize();
    var c = targetCount();
    while (blocks.length < c) blocks.push(makeBlock(true));
    if (blocks.length > c + 4) blocks.length = c;
  }, 250));
}

// ─── HERO ──────────────────────────────────────────────────────────────────────
// Word-by-word title reveal + eyebrow + subtitle + CTA + image

function animateHero() {
  var h1 = document.querySelector('.hero--home h1');
  var eyebrow = document.querySelector('.hero__eyebrow');
  var sub = document.querySelector('.hero__sub');
  var actions = document.querySelector('.hero__actions');
  var heroImg = document.querySelector('.hero-img img, .hero-img .img-placeholder');

  // ── Split H1 into word spans ──────────────────────────────────────────────
  if (h1) {
    // Preserve <br> tags: split on them first, wrap words in each segment
    var rawHtml = h1.innerHTML;
    var lines = rawHtml.split(/<br\s*\/?>/i);
    var wrappedLines = lines.map(function (line) {
      return line.replace(/(\S+)/g, function (word) {
        return "<span class=\"hero-word\"><span class=\"hero-word__inner\">".concat(word, "</span></span>");
      });
    });
    h1.innerHTML = wrappedLines.join('<br>');
    var wordInners = h1.querySelectorAll('.hero-word__inner');
    gsap__WEBPACK_IMPORTED_MODULE_1__.gsap.set(wordInners, {
      y: '115%'
    });
    gsap__WEBPACK_IMPORTED_MODULE_1__.gsap.to(wordInners, {
      y: '0%',
      stagger: 0.055,
      duration: 0.75,
      ease: 'power3.out',
      delay: 0.55
    });
  }

  // ── Eyebrow ───────────────────────────────────────────────────────────────
  if (eyebrow) {
    gsap__WEBPACK_IMPORTED_MODULE_1__.gsap.from(eyebrow, {
      opacity: 0,
      y: 18,
      duration: 0.6,
      ease: 'power2.out',
      delay: 0.3
    });
  }

  // ── Sub-heading ───────────────────────────────────────────────────────────
  if (sub) {
    gsap__WEBPACK_IMPORTED_MODULE_1__.gsap.from(sub, {
      opacity: 0,
      y: 22,
      duration: 0.7,
      ease: 'power2.out',
      delay: 0.9
    });
  }

  // ── CTA buttons ───────────────────────────────────────────────────────────
  if (actions) {
    gsap__WEBPACK_IMPORTED_MODULE_1__.gsap.from(actions.children, {
      opacity: 0,
      y: 16,
      stagger: 0.1,
      duration: 0.6,
      ease: 'power2.out',
      delay: 1.05
    });
  }

  // ── Hero image ────────────────────────────────────────────────────────────
  if (heroImg) {
    gsap__WEBPACK_IMPORTED_MODULE_1__.gsap.fromTo(heroImg, {
      opacity: 0,
      y: 36,
      scale: 0.97
    }, {
      opacity: 1,
      y: 0,
      scale: 1,
      duration: 1.1,
      ease: 'power3.out',
      delay: 0.65
    });
  }

  // ── Header drop-in ────────────────────────────────────────────────────────
  var headerInner = document.querySelector('.js-site-header .site-header__inner');
  if (headerInner) {
    gsap__WEBPACK_IMPORTED_MODULE_1__.gsap.from(headerInner, {
      y: -40,
      opacity: 0,
      duration: 0.9,
      ease: 'power2.out',
      delay: 0.1
    });
  }
}

// ─── SCROLL REVEAL ─────────────────────────────────────────────────────────────

function animateElements() {
  // Parallax hero image scroll-out
  gsap__WEBPACK_IMPORTED_MODULE_1__.gsap.utils.toArray(".parallax-up").forEach(function (item) {
    var overlay = $(item).find('.hero--overlay');
    var img = $(item).find('img');
    gsap__WEBPACK_IMPORTED_MODULE_1__.gsap.to(overlay, {
      opacity: 1,
      ease: "none",
      scrollTrigger: {
        trigger: item,
        scrub: 0.2,
        start: "top top",
        end: "bottom 20%"
      }
    });
    gsap__WEBPACK_IMPORTED_MODULE_1__.gsap.to(img, {
      opacity: 0,
      scale: 1.2,
      ease: "none",
      scrollTrigger: {
        trigger: item,
        scrub: 0.2,
        start: "top top",
        end: "bottom 10%"
      }
    });
  });

  // ── .animated — horizontal slide-in ─────────────────────────────────────
  gsap__WEBPACK_IMPORTED_MODULE_1__.gsap.utils.toArray(".animated").forEach(function (elem) {
    var imgs = $(elem).find("img");
    gsap__WEBPACK_IMPORTED_MODULE_1__.gsap.set(elem.children, {
      opacity: 0
    });
    imgs.each(function () {
      var $img = $(this);
      var isCarousel = $img.closest('.js-card-carousel').length > 0;
      var isHero = $img.closest('.parallax-up').length > 0;
      if (!isCarousel && !isHero) {
        gsap__WEBPACK_IMPORTED_MODULE_1__.gsap.set($img, {
          scale: 1.08
        });
      }
    });
    gsap_ScrollTrigger__WEBPACK_IMPORTED_MODULE_2__.ScrollTrigger.create({
      trigger: elem,
      start: "top 90%",
      once: true,
      onEnter: function onEnter() {
        gsap__WEBPACK_IMPORTED_MODULE_1__.gsap.fromTo(elem.children, {
          x: 40,
          opacity: 0
        }, {
          x: 0,
          opacity: 1,
          stagger: 0.1,
          duration: 0.7,
          ease: 'power2.out',
          clearProps: "transform, opacity"
        });
        imgs.each(function () {
          var $img = $(this);
          if (!$img.closest('.js-card-carousel').length) {
            gsap__WEBPACK_IMPORTED_MODULE_1__.gsap.to($img, {
              scale: 1,
              duration: 1,
              ease: "power2.out",
              clearProps: "transform"
            });
          }
        });
      },
      onEnterBack: function onEnterBack() {
        gsap__WEBPACK_IMPORTED_MODULE_1__.gsap.to(elem.children, {
          duration: 0.3,
          opacity: 1,
          stagger: 0.05
        });
      }
    });
  });

  // ── .animated-up — upward fade-in, slightly staggered ───────────────────
  gsap__WEBPACK_IMPORTED_MODULE_1__.gsap.utils.toArray(".animated-up").forEach(function (elem) {
    var children = elem.children;
    gsap_ScrollTrigger__WEBPACK_IMPORTED_MODULE_2__.ScrollTrigger.create({
      trigger: elem,
      start: "top 88%",
      once: true,
      immediateRender: false,
      onEnter: function onEnter() {
        gsap__WEBPACK_IMPORTED_MODULE_1__.gsap.fromTo(children, {
          y: 40,
          opacity: 0
        }, {
          y: 0,
          opacity: 1,
          stagger: 0.12,
          duration: 0.75,
          ease: "power3.out",
          clearProps: "transform, opacity"
        });
      },
      onEnterBack: function onEnterBack() {
        gsap__WEBPACK_IMPORTED_MODULE_1__.gsap.to(children, {
          opacity: 1,
          duration: 0.3,
          stagger: 0.05
        });
      }
    });
  });
}

// ─── PRICING CARDS ─────────────────────────────────────────────────────────────
// Custom entrance: Free card slides from left, Pro card from right with a glow pulse

function animatePricingCards() {
  var pricingSection = document.querySelector('.pricing-cards');
  if (!pricingSection) return;
  var _pricingSection$query = pricingSection.querySelectorAll('.pricing-card'),
    _pricingSection$query2 = _slicedToArray(_pricingSection$query, 2),
    freeCard = _pricingSection$query2[0],
    proCard = _pricingSection$query2[1];
  if (!freeCard || !proCard) return;
  gsap__WEBPACK_IMPORTED_MODULE_1__.gsap.set(freeCard, {
    opacity: 0,
    x: -30
  });
  gsap__WEBPACK_IMPORTED_MODULE_1__.gsap.set(proCard, {
    opacity: 0,
    x: 30
  });
  gsap_ScrollTrigger__WEBPACK_IMPORTED_MODULE_2__.ScrollTrigger.create({
    trigger: pricingSection,
    start: 'top 78%',
    once: true,
    onEnter: function onEnter() {
      var tl = gsap__WEBPACK_IMPORTED_MODULE_1__.gsap.timeline();
      tl.to(freeCard, {
        opacity: 1,
        x: 0,
        duration: 0.75,
        ease: 'power3.out'
      }).to(proCard, {
        opacity: 1,
        x: 0,
        duration: 0.75,
        ease: 'power3.out'
      }, '-=0.5')
      // Glow pulse on Pro card after it lands
      .fromTo(proCard, {
        boxShadow: '0 0 0 1px rgba(2,172,233,0.08), 0 32px 80px rgba(2,116,165,0.28), 0 8px 24px rgba(0,0,0,0.3)'
      }, {
        boxShadow: '0 0 0 1px rgba(2,172,233,0.25), 0 32px 100px rgba(2,116,165,0.5), 0 8px 24px rgba(0,0,0,0.3)',
        duration: 0.5,
        ease: 'power2.out',
        yoyo: true,
        repeat: 1
      }, '-=0.1');
    }
  });
}

// ─── MAGNETIC BUTTONS ──────────────────────────────────────────────────────────
// Subtle cursor-tracking pull on primary CTA buttons

function initMagneticButtons() {
  var buttons = document.querySelectorAll('.button--primary, .button--nav');
  buttons.forEach(function (btn) {
    btn.addEventListener('mousemove', function (e) {
      var rect = btn.getBoundingClientRect();
      var cx = rect.left + rect.width / 2;
      var cy = rect.top + rect.height / 2;
      var dx = e.clientX - cx;
      var dy = e.clientY - cy;
      gsap__WEBPACK_IMPORTED_MODULE_1__.gsap.to(btn, {
        x: dx * 0.18,
        y: dy * 0.18,
        duration: 0.35,
        ease: 'power2.out'
      });
    });
    btn.addEventListener('mouseleave', function () {
      gsap__WEBPACK_IMPORTED_MODULE_1__.gsap.to(btn, {
        x: 0,
        y: 0,
        duration: 0.6,
        ease: 'elastic.out(1, 0.55)'
      });
    });
  });
}
function refreshAnimations() {
  gsap_ScrollTrigger__WEBPACK_IMPORTED_MODULE_2__.ScrollTrigger.refresh();
}

/***/ })

}]);
//# sourceMappingURL=assets_src_js_scripts_animations_js.min.js.map